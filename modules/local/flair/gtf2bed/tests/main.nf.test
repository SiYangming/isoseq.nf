nextflow_process {

    name "Test Process FLAIR_GTF2BED"
    script "../main.nf"
    process "FLAIR_GTF2BED"

    tag "modules"
    tag "flair"
    tag "flair/gtf2bed"

    test("gtf2bed - gtf") {
        
        when {
            process {
                """
                // Use consistent test data
                def gtf = file("\${projectDir}/modules/local/flair/testdata/basic.annotation.gtf", checkIfExists: true)

                input[0] = [ 
                    [ id:'test', single_end:false ], 
                    gtf
                ]
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(process.out).match() }
            )
        }

    }

}
